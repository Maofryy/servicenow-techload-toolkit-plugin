# ACL Security Matrix — Template

Use this structure for Layer 2 of the ACL Logic Architect output. Produce one table per table in the export.

---

## Security Matrix — `{table_name}`

> {n} ACL records analyzed | Operations covered: {comma-separated list} | Inactive ACLs included: {yes / no}

| Field / Record | Operation | Decision Type | Decision | Roles Required | Script | SAC | Admin Bypass |
|---|---|---|---|---|---|---|---|
| `*` (record) | {operation} | {Allow If / Deny Unless} | {✅ allow / ❌ deny} | {role1, role2 / —} | {— / ⚙️ Yes} | {— / SAC_xxxxx} | {Yes / No} |
| `{field}` (field) | {operation} | {Allow If / Deny Unless} | {✅ allow / ❌ deny} | {role1 / —} | {— / ⚙️ Yes} | {— / SAC_xxxxx} | {Yes / No} |

**Legend:**
- Decision Type = the ACL's evaluation logic (Allow If / Deny Unless) | Decision = the net access outcome (✅ / ❌)
- ✅ allow = Allow If ACL | ❌ deny = Deny Unless ACL
- ⚙️ = script-based logic — plain-English translation follows immediately below this table
- [SAC] = auto-generated by the Security Attribute Control system — review before modifying
- Admin Bypass = `admin_overrides` is true — admin users bypass this ACL

### Script Translation (include for every ⚙️ row, immediately after the table)

**`{table.field}` — {operation} ({Allow If / Deny Unless}):**
> {Plain-English translation. Frame with decision type:}
> - Allow If: "Grants access when [what the script checks]"
> - Deny Unless: "Blocks access unless [what the script checks]"

Risk flags:
<!-- 🔴 if: script has no `answer = false` default at the top
     🔴 if: script calls .getValue() on a reference field without a null check
     🟡 if: script uses gs.hasRole('admin') without further restriction
     Omit this line entirely if none apply. -->
{🔴 / 🟡 if applicable — or omit section if none}

---
*Repeat this block for each table in the export.*
